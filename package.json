{
  "name": "event-register-manager-sources",
  "version": "1.1.0",
  "description": "Sources related to the package custom event register manager (cermjs)",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "type-check": "tsc --noEmit",
    "type-check:watch": "npm run type-check -- --watch",
    "build": "npm run build:types && npm run build:js && npm run postbuild:config",
    "postbuild:config": "cp config/package.json LICENSE README.md lib",
    "build:types": "tsc",
    "build:js": "babel src --out-dir lib --extensions \".ts,.tsx\" --source-maps inline"
  },
  "keywords": [],
  "author": "Ben Lmsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/ARKHN3B/window-event-manager.git"
  },
  "bugs": "https://github.com/ARKHN3B/window-event-manager/issues",
  "license": "GNU General Public License v3.0",
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/plugin-proposal-class-properties": "^7.12.1",
    "@babel/preset-env": "^7.8.3",
    "@babel/preset-typescript": "^7.8.3",
    "uglify-js": "^3.11.2"
  }
}
